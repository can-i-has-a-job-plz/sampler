sudo: false
language: ruby
rvm:
  - 2.3.1
before_install: gem install bundler -v 1.13.6

services:
  - "postgresql"

addons:
  postgresql: "9.4"

before_install:
  - "rm -f ${BUNDLE_GEMFILE}.lock"
  - "gem update bundler"

before_script:
  - bundle update

env:
  matrix:
    - "RAILS_VERSION=5.0.0"
